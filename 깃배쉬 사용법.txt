git remote -v (레퍼지토리 확인)

git remote remove origin(레퍼지토리 삭제)

git remote add origin 주소.git

git branch 이름 (브랜치 생성)

git checkout 브랜치 이름 (해당 브랜치로 변경)



*업로드
{
git add .

git commit -m test

git push origin 브랜치이름
}



*merge
{
git checkout master

git merge 브랜치이름

git push origin master
}






